#!/bin/bash

export LD_LIBRARY_PATH=../lib
export CLASSPATH=classes:../lib/antlr.jar:../lib/com.microsoft.z3.jar
while true
do
    java -Djava.library.path=../lib Run $1 4 20 30 2>/dev/null
    [[ $? == 0 ]] && break
done
